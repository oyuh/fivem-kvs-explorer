# web

The SvelteKit front-end for KVS Explorer. See the [root README](../README.md) for the
full docs; the short version is you run everything from the repo root with Bun:

```sh
bun run setup   # install dependencies
bun run dev     # build the wasm core and start Vite
bun run build   # production build into web/build/
```
