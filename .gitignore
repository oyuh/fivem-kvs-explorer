# Rust / Cargo build output
/target
**/target

# Node / Bun dependencies
**/node_modules

# SvelteKit / Vite
/web/.svelte-kit
/web/build
/web/dist
/web/.vite

# NOTE: web/src/lib/wasm (the wasm-pack output) is intentionally committed so
# Vercel can build the site without a Rust toolchain. Rebuild it with
# `bun run wasm` and commit the result whenever the Rust crate changes.

# Vercel CLI
.vercel

# Logs
*.log
# ...except the demo fixture's WAL, which the deployed sample store needs.
!web/static/sample/*.log
npm-debug.log*
bun-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
